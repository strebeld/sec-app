apiVersion: v1
kind: Service
metadata:
  name: todo-backend
  namespace: sec-app
  labels:
    app: mongo-todo
spec:
  selector:
    app: mongo-todo
    tier: backend
  ports:
    - name: http
      port: 80
      targetPort: http
  type: LoadBalancer